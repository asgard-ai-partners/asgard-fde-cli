# db-query 的 driver。**不必全裝** —— 只裝這個 engagement 真的會連的那幾個;
# query.py 是 lazy import 的,沒裝的 class 只有在你用到它的時候才會報錯,而且會
# 直接告訴你要裝哪一個。
#
# 一次裝齊:
#   python3 -m venv .venv
#   .venv/bin/pip install -r .agents/skills/db-query/scripts/requirements.txt

psycopg[binary]>=3   # postgres
PyMySQL>=1.1         # mysql
pymssql>=2.3         # mssql
oracledb>=2          # oracle -- thin 模式,不需要 Oracle Instant Client
trino>=0.330         # trino
PyAthena>=3          # athena
pyjwt[crypto]>=2.8   # netsuite -- JWT client assertion 需要 ES256

# salesforce 不在這裡:它走標準函式庫的 REST,沒有相依。
# hana 也不在:見 ../references/connectors.md。
