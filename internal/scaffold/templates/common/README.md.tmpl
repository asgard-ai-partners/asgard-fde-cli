# common — 跨 project 的共用資產

每個 project 是一份獨立的 Helm chart。這個資料夾放**跨 project 共用**的東西:目前只有
自定義技能來源。

project 結構、部署宣告(`.asgard-pipeline.yaml`)、rollout 請看根目錄的 `README.md` 與
`AGENTS.md`(後者是 coding agent 的 SoT;`CLAUDE.md` 只是 `@AGENTS.md` 指標)。

## 內容

```
common/
  skills/<skill>/SKILL.md   # 跨 project 共用的「自定義技能」來源
```

**這裡不再有 values 檔。** 值不在 repo 裡:`.asgard-pipeline.yaml` 宣告有哪些 key,值填在
Platform 上,Run 的時候才注入。

```bash
asgard-cli pipeline variables list --release <name>
asgard-cli pipeline variables set  --release <name> <key> <value>
```

跨 project 共用的疊加點也跟著消失了 —— 兩個 Release 之間的差異就是各自填的值,不是兩個
檔案的疊加順序。本地渲染是 `asgard-cli render <release>`,用佔位值,只渲染不部署。

## `skills/` — 自定義技能的單一來源

我們自己寫的技能放這裡,一個技能一個目錄、內含 `SKILL.md`:

```
common/skills/
  <skill>/SKILL.md
```

每個 SkillSet 有**自己專屬的一組 SourceSet + Syncer**(三件一套寫在同一個
`templates/skill_set/<name>.yaml` 裡,以 `---` 分隔)。Syncer 把 repo clone 到固定的 `git/`
member,SkillSet 再以 `searchPaths` 從那底下挑要裝備哪幾個 —— **逐個技能目錄列出,不要只列
父目錄**(平台以「一個 searchPath = 一個技能目錄」解析,列父目錄會靜靜地解析成零個技能):

```yaml
spec:
  sourceSetName: ss-sk-internal          # 這個 SkillSet 專屬的 SourceSet
  searchPaths:
    - git/common/skills/<skill>   # git/ 之後就是 repo 根目錄
```

再由 `Agent.managed.skillSetNames` 掛上 SkillSet。

> 為什麼是一對一:平台的 `POST /v1/skill-set/from-git` 一次建出 SkillSet + SourceSet + Syncer
> 並互綁,Platform UI 照這個結構顯示。多個 SkillSet 共用一個 SourceSet 的話,UI 找不到
> 「這個技能集的 git 設定」。完整說明見 AGENTS.md 的 **Skills** 一節。

> [!IMPORTANT]
> **`common/skills/` 是 runtime 技能;`.agents/skills/` 是 design-time 技能。** 兩者是完全不同的機制:
> - `common/skills/<skill>/SKILL.md` —— 給**部署後的 agent**。要經 SourceSet 同步進平台、由
>   `SkillSet` CR 以 `searchPaths` 選取、再由 `Agent.managed.skillSetNames` 掛上才會生效。
> - `.agents/skills/<skill>/SKILL.md` —— 給**在這個 repo 工作的 coding agent**(例如
>   `semantic-layer-modeling`、`asgard-cr-verification`)。永遠不會進平台。
>
> 寫技能前先確定要的是哪一種:部署後 agent 需要的領域知識放 `common/skills/`;開發流程的操作
> 指引放 `.agents/skills/`。

> Anthropic 公開技能(pdf / docx / pptx / xlsx)由各 project 自己的 `syn-sk-base`
> 從 `anthropics/skills` 另外同步,**不放在本 repo**。

## 本地渲染與驗收

```bash
asgard-cli render internal dev            # 渲染檢視(manifest 走 stdout)
asgard-cli render internal dev > .out/rendered.yaml
asgard-cli verify internal                # 渲染 + 驗收不變量,一步到底
```

chart 從 `.asgard-pipeline.yaml` 裡該 Release 的宣告讀,
所以本地渲染出來的就是 CD 會部署的東西;疊加順序、`--namespace`、release 名也都一致。
未宣告該 env 的 project 會被明確拒絕,而不是猜一個 namespace。

只需要 `helm` 在 PATH 上 —— 不需要 bash,也不需要 `yq`。用 `asgard-cli doctor` 確認。

> [!IMPORTANT]
> **部署一律走 CD,不要從本機 `helm upgrade`。** `syn-sk-internal` 以
> `revision: {{ .Chart.AppVersion }}` 同步本 repo,而只有 CI 會把發布 tag 蓋進 `appVersion`。
> 本機安裝會把 `Chart.yaml` 裡的佔位版本(`0.1.0`)當成 git ref 寫進 Syncer,那個 ref 不存在 →
> Syncer 每次都同步失敗。所以 `asgard-cli render` **只渲染、沒有安裝路徑**。


## 不放這裡的東西

- **project environment id**:Platform 注入 `.Values.asgard.projectEnvironmentId`,不用填也不能填。
- **namespace / 要部署哪些 env / 用哪個 values 檔**:per-project → 放
  `.asgard-pipeline.yaml`。
- **密鑰**:走 cluster Secret(`appSecretName`,預設 `app-secret`),不進 values。

## 這個資料夾的定位

- **這個 repo 讀的是客戶實際存在的資料庫**,不是由 `.sql` 生出來的 demo 資料。
  所以寫 `SemanticLayer` 之前要真的連上去 introspect,不要照著文件猜 schema ——
  `.agents/skills/db-query/` 是連線與內省的工具,
  `.agents/skills/semantic-layer-modeling/` 說明怎麼把看到的東西變成 CR。
- 版面是一個 project 一個 Helm chart、一個 namespace,全部放在 `projects/<project>/`。
  project 的數量與拆分由客戶的 audience 決定(見 `AGENTS.md`),不是固定的。
