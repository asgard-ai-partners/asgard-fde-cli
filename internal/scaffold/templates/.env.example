# .env — 本地開發連線設定(複製成 .env 填真值;.env 已 gitignore)
#
# 這裡的資料庫是客戶的**實際**業務系統,一律唯讀。
# 密碼平時走 cluster Secret `app-secret`,可用下列指令取得明文:
#   kubectl get secret app-secret -n <namespace> -o jsonpath='{.data.<key>}' | base64 -d
#
# 每接上一個資料庫,就在這裡補一組,並同時:
#   1. 在 scripts/db/pgenv.py 的 DB_TARGETS 註冊這個 target
#   2. 在對應 project 的 chart/app/templates/data_connector/ 新增 DataConnector CR
#   3. 非密碼欄位同時宣告進 .asgard-pipeline.yaml 的 chartValues,值用
#      `asgard-cli pipeline variables set` 填在 Platform 上
#
# 格式(<TARGET> 用大寫,對應 DB_TARGETS 裡的小寫 target 名):
#
#   # --- <系統名>(<PostgreSQL|MSSQL>) --- secret key: <target>_db_password
#   <TARGET>_DB_HOST=
#   <TARGET>_DB_PORT=
#   <TARGET>_DB_USER=
#   <TARGET>_DB_NAME=
#   <TARGET>_DB_PASSWORD=
#   <TARGET>_DB_SSLMODE=       # 只有 PostgreSQL 需要
#
# NetSuite 沒有 DBAPI driver,走 SuiteQL over REST,所以它是另一組鍵
# (見 .agents/skills/semantic-layer-modeling/SKILL.md):
#
#   NS_HOST=
#   NS_CONSUMER_KEY=
#   NS_CERTIFICATE_ID=
#   NS_PRIVATE_KEY_PEM=
#   NS_SIGNATURE_ALGORITHM=
