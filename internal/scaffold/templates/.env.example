# .env -- design time 的連線設定(複製成 .env 填真值;.env 已 gitignore)
#
# 這裡是**設計期**的憑證:給在這台筆電上工作的 coding agent 用的,讓它能連上客戶
# 的系統看清楚 schema 與資料現況。它跟另外兩種憑證不是同一回事:
#
#   design-time         這個檔案            客戶或 FDE 填          .env,不進版控
#   pipeline variables  Platform 上          asgard-cli pipeline variables set
#   runtime secret      叢集裡的 K8s Secret  Platform 自己建立與維護
#
# **值可能是同一個字串**(同一個資料庫),但設定的方式完全不同,不要混為一談。
# 尤其:**不要為了拿設計期的密碼去讀叢集的 Secret** —— 跟系統的擁有者要。
#
# 每一組連線用自己的前綴,前綴由你決定 —— 一個 repo 可以同時接兩個 PostgreSQL,
# 它們本來就是兩群 key-value,不可能共用名稱。鍵名的字尾**現在**跟 DataConnector
# CRD 的欄位同名(sslMode -> SSL_MODE),所以填在這裡的東西可以一格一格搬進 CR ——
# 那是一個方便,不是一個契約,兩邊分岔是可以的。
#
# 產生某個 class 需要的鍵(直接接到這個檔後面):
#
#   .venv/bin/python .agents/skills/db-query/scripts/query.py \
#     --class postgres --prefix UOF_DB_ --keys >> .env
#
# 然後請憑證的持有人自己填 —— **不要在對話裡跟人要密碼**:
#
#   asgard-cli local-env
#
# 它在 127.0.0.1 開一頁表單,存檔後只回報「哪些鍵有值了」,不回報值。
#
# 八個 class 各自需要什麼、以及怎麼內省,見:
#   .agents/skills/db-query/SKILL.md
#   .agents/skills/db-query/references/connectors.md
#
# 例(PostgreSQL,前綴 UOF_DB_):
#
#   UOF_DB_HOST=
#   UOF_DB_PORT=
#   UOF_DB_USER=
#   UOF_DB_PASSWORD=
#   UOF_DB_DATABASE=
#   UOF_DB_SSL_MODE=
#
# 接上一個系統之後,還有兩件事要做:
#   1. 在對應 project 的 chart/app/templates/data_connector/ 新增 DataConnector CR
#   2. 非密碼欄位宣告進 .asgard-pipeline.yaml 的 chartValues,值用
#      `asgard-cli pipeline variables set` 填在 Platform 上
