---
name: asgard-fde-onboarding
description: Use FIRST when working in this repo - it routes you to the right command, skill and document for whatever you are about to do, and states the rules that hold at every stage. Covers starting an onboarding, splitting the work into projects, wiring up a customer database, writing Asgard custom resources, and running the acceptance gate.
---

# <<.Workspace.Name>> onboarding

> **Design time.** This skill is for the coding agent working in this repo. It is
> never synced into the platform. Skills the *deployed* agent uses at runtime
> live in `common/skills/` and reach the cluster through a `SkillSet` - a
> completely separate mechanism. Do not confuse the two.

This repo defines the Asgard custom resources (`asgard-ai.com/v1alpha1`) for
<<.Workspace.Name>>, one Helm chart per project, each deployed to its own
namespace. There is no application source code.

## Start here, every session

```bash
asgard-cli question      what nobody has answered yet, and who each is with
asgard-cli request       what the customer asked for and is not done
asgard-cli task          the task specs that are open
asgard-cli project       what each chart declares, and what its shape lacks
```

Each reads one file the repository keeps, or the CR kinds a chart declares,
**from the repository itself** - so all four stay right when something was done
by hand. Each takes `--format json`.

**Read the questions first**, and do not design past one of them.

**No command here says where the onboarding is.** There used to be one, and it
was wrong in a way that cost this engagement: it derived a position from the
earliest missing CR kind, and an onboarding is not linear - three of the most
expensive decisions were made once, built, and reversed after contact with
reality. A tool that walks you into the same first answer is the failure mode,
not the goal.

So form your own view of where things stand, and reach for the guidance by
subject:

```bash
asgard-cli find "<what you are actually deciding>"
```

That searches the platform wiki, the deployment shapes, the decision guidance and
the skills together, so "how is the read path decided" reaches the material that
covers it without having to have arrived anywhere first. **None of it is a step**
- `asgard-cli guide` lists every piece of it, and any one can be read at any
time, as many times as it is useful.

**Do not reproduce what any of it says.** Run it and read it: the guidance is
versioned with the tool, while a copy pasted into a transcript goes stale.

## Where the material is

Not a sequence to follow - a map of what answers what.

| what you are doing | where to look |
|---|---|
| **about to talk to the customer** | `asgard-cli brief customer-meeting` - five things said wrong to a customer, each because somebody said it. The riskiest thing here leaves no trace in this repo, so nothing in the repo will remind you |
| about to touch a chart, or hand it over | `asgard-cli brief write-chart`, `asgard-cli brief handover` |
| anything, first | `asgard-cli question`, then `request`, `task` and `project` |
| **the customer said something and you have to turn it into work** | `asgard-cli guide requirements` - the interview, then `asgard-cli request add "<their words>"` |
| **looking something up** | `asgard-cli find "<the customer's words>"` - searches both bodies of reference material and hands you the other half |
| **about to write a CR** | `asgard-cli usecase <shape>` - the skeleton and its traps |
| what a thing on the platform even is | `asgard-cli wiki <page>` - read this before the extract, which assumes it |
| how to change this repo - layout, conventions, CRD traps | `AGENTS.md` |
| what the system currently does | `docs/spec/<<.SpecSlug>>/` |
| why something was decided | `docs/decisions/` |
| **reading a customer's own system** - list tables, check a column's real values, verify a join | `db-query` skill. Eight connector classes, read-only |
| turning what you found into a `SemanticLayer` or `DataConnector` | `semantic-layer-modeling` skill |
| **somebody has to give you a credential** | `asgard-cli local-env` - opens a form for them to type it into. **Never ask for a password in the conversation** |
| before committing anything under `projects/*/chart` or `common/` | `asgard-cr-verification` skill (`asgard-cli skill update` writes it) |
| work that needs a spec before implementation | `spec-workflow` skill |
| a slide deck for the customer | `proposal-deck` skill |
| **any 繁體中文 the customer will read** - a slide, a reply to their question, a cube `description`, an agent's prompt | `plain-chinese` skill. Load it for the second pass, not while drafting |
| registering a new project | `asgard-cli project add <slug>` - it writes the chart skeleton too |
| the repo has drifted: contradictions, stale claims, a term nobody owns | `knowledge-base` skill, after `asgard-cli check` |

## The question behind the three big decisions

The read path, the entry point, and where unstructured knowledge lives all
follow from one question:

> **Who is on the other end - an internal caller that can authenticate, or an
> anonymous visitor?**

An internal audience and a public one need different shapes, and that is not a
preference: it follows from what the platform can and cannot do. Each of the
three has an obvious answer that has already been proven wrong in production.
`asgard-cli guide read-path`, `entry-point` and `knowledge` each state
the reversal, and `next` raises them when this repository meets their condition;
`AGENTS.md` has the same three under "The three decisions that get answered
wrong".

**Establish the audience before designing anything.**

## Rules that hold at every stage

- **Never guess a schema.** Connect to the real database and introspect it.
  Reasoning about a schema is not verifying it. The `db-query` skill is how.
- **Never ask anybody to tell you a password.** Not in the conversation, not
  "paste it and I will remove it after" - a credential that has been through a
  transcript has to be treated as disclosed. Write the keys you need into
  `.env` with empty values, then run `asgard-cli local-env`: it opens a form
  for whoever holds the credential, and tells you only which keys were filled
  in. **Re-read `.env` afterwards** - they can add keys you did not ask for,
  and that is the point of letting them.
- **Source systems are read-only** until a spec says otherwise. Any write path
  points outward, never at a source system, and needs human approval.
- **Run the gate before committing** anything under `projects/*/chart` or
  `common/`, and stop at the first red step.
- **Write the decision down the day it is settled** -
  `docs/decisions/YYYY-MM-DD-<topic>.md`, saying why the rejected option was
  rejected. That sentence is the only part nobody can reconstruct later.
- **A behaviour change is not done** until the delta reaches
  `docs/spec/<<.SpecSlug>>/`. A task spec stops being read once it is done; the
  living spec is what the next person reads.

**Checked:** 2026-09-04 - every command this page routes to exists and answers
what is claimed, checked by running each. The one platform statement it makes is
the distinction between a runtime skill under `common/skills/` bound by a
`SkillSet` CR and a design-time skill under `.agents/skills/` that only the
coding agent reads; `SkillSet` is in the contract at asgard-kube `15ded0f` and
the two mechanisms are separate, which is why the warning is at the top.

**Unchecked:** the routing itself - which question should reach which command.
That is one engagement's map of its own material, and its failure mode is
silent: a reader sent to the wrong page reads a correct document and answers a
different question. `asgard-cli reading` measures the half of that which is
measurable, and `asgard-cli reading --misses` records the searches this material
did not answer.
