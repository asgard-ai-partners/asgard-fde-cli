# docs/decisions/ — 決議紀錄(dated decision records)

> dated、**不可變**的決議紀錄(ADR 的概念)。每一份記錄某個時間點「**定了什麼 / 為什麼 /
> 動到哪些模組 / 來源**」,外加還沒收斂的問題。

## 邊界(這裡是什麼、不是什麼)

| 資料夾 | 角色 | 時態 |
|--------|------|------|
| [`../meeting-notes/`](../meeting-notes/) | raw 討論 / issue 輸入 | 收斂**前** |
| **`docs/decisions/`(本目錄)** | **收斂後的決議(為什麼 + 歷史)** | 某時間點的快照 |
| [`../spec/`](../spec/) | living spec(當下唯一行為) | **永遠現狀** |
| [`../../requirements/tasks/`](../../requirements/tasks/) | 可執行的工作單位 | 一件工作的生命週期 |

**決議紀錄與任務規格不是同一件事。** 任務規格回答「這件工作怎麼做、做完怎麼驗」,寫得很長,
而且會隨著實作被回填;決議紀錄回答「當初為什麼這樣定」,寫得很短,而且**寫完就不動**。
一份任務規格常常帶出好幾個決議 —— 那就寫好幾份。

## 規則

- **不可變**:改變主意 → 寫**新的**一份,不回頭改舊的。
  (唯一例外:搬家之後修相對連結這種機械性更新。)
- 檔名:`YYYY-MM-DD-<topic>.md`,日期是**決議成立的那天**,不是寫這份文件的那天。
- 範本:[`_decision-template.md`](_decision-template.md)。
- 一份只寫**一個**決議。內容變成「這次改了七件事」時,拆成七份。
- 每一份都要在 living spec 那一側留下痕跡:模組頂端的「歷史與理由見 …」要連得回來,
  而且 [`../spec/<<.SpecSlug>>/README.md`](../spec/<<.SpecSlug>>/README.md) 的追溯表要有它。

## 從哪裡開始

這個資料夾一開始是空的。第一份決議在導入過程中第一次「定了什麼」的時候寫下 ——
常見的第一批是:project 怎麼拆、每個 project 的入口形狀、讀取路徑走語意層還是固定查詢工具。

**在當下就寫。** 事後回填只能重述,補不回當時被否決的選項為什麼被否決,
而那正是這份文件唯一無可取代的內容。
