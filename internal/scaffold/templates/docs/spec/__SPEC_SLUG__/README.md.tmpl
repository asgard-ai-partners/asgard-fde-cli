# <<.Workspace.Name>> —— Living Spec

這套系統**當下**的行為規格。收斂一次改寫一次,永遠只反映現狀。

> 「怎麼改這個 repo」在 [`../../../AGENTS.md`](../../../AGENTS.md);
> 「當初為什麼這樣定」在 [`../../decisions/`](../../decisions/);
> 「要做的工作單位」在 [`../../../requirements/tasks/`](../../../requirements/tasks/)。

## 模組索引

一個模組對齊一個能力域,大小約等於一次會被整批討論或整批改動的範圍。
狀態:✅ 已蒸餾成完整段落 / 🟡 skeleton,下次收斂時補完。

| 模組 | 狀態 | 涵蓋 |
|---|---|---|

> 還沒有模組。第一個通常是 `architecture.md` —— 在 project 拆分與讀取路徑定案之後寫,
> 記錄不變量(哪些來源是唯讀的、寫入路徑指向哪裡、每個 project 的入口形狀)。
>
> **新增模組時,同一次改動要把它加進上面這張表。**
>
> 模組本身用繁體中文寫,規則見
> [`.agents/skills/plain-chinese/`](../../../.agents/skills/plain-chinese/)。
> 這是全 repo 讀最久的文件:一句「為整體架構奠定了良好基礎」半年後對任何人
> 都沒有用,而「訂單資料只讀不寫,寫入只指向 CRM」半年後還是可以拿來核對。
> `asgard-cli check` 會比對這張索引與目錄裡實際的 `.md` 檔,對不上就是紅的。

## 追溯表

每個模組連回成立它的決議紀錄。`asgard-cli decision add` 會在寫下記錄的同時補上一列,
所以下面那行 HTML 註解是它用來定位這張表的錨點 —— 不要刪。

<!-- traceability -->

| 決議 | 日期 | 影響的模組 |
|---|---|---|
