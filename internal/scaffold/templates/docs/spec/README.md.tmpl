# docs/spec/ — Living Spec(當下唯一的系統行為規格)

這裡放**這套系統當下講定的行為**。收斂一次就改寫一次,**永遠反映現狀**。

> 這是「系統做什麼」的事實來源。「怎麼改這個 repo」在 [`AGENTS.md`](../../AGENTS.md);
> 「當初為什麼這樣定」在 [`../decisions/`](../decisions/);
> 「要做的工作單位」在 [`../../requirements/tasks/`](../../requirements/tasks/)。

## 當前 spec

| slug | living spec | 涵蓋 |
|---|---|---|
| `<<.SpecSlug>>` | [`<<.SpecSlug>>/`](<<.SpecSlug>>/) | 本 repo 全部 Asgard CR 的行為 |

**一個 slug 通常就夠。** 各 project 共用一條 `TASK-` 號、共用一套平台慣例、共用一組驗收閘門,
拆成多個 slug 只會讓跨 project 的規則(入口形狀、技能機制、部署)無家可歸。
只有在出現受眾與生命週期都獨立的第二套系統時,才開第二個 slug。

## 寫作慣例

- **語言**:繁體中文,標點沿用本 repo 既有慣例(半形 `,` `(` `)`,全形 `。` `、` 「」)。
- **只寫行為與規則**:能力邊界、資料口徑、路由判準、狀態機、不變量、安全論證。
- **不寫**:YAML 欄位怎麼填、Helm 模板怎麼寫、CRD 有哪些 gotcha —— 那些是 `AGENTS.md`。
  判準是「刪掉這段之後,是行為會做錯,還是 chart 會寫壞?」後者不屬於這裡。
- **不寫歷史**:不寫「原本是 A,後來改成 B」。只寫 B,並在段落旁連到那份決議紀錄。
- **每個模組對齊一個能力域**,大小約等於一次會被整批討論或整批改動的範圍。
- **數字要可追**:凡是列數、cube 數、欄位數這類事實,寫下前先實際查證(渲染 chart 或查資料庫),
  並在旁註明量測日期。過期的精確數字比沒有數字更糟。
- 每個模組頂端放一行「歷史與理由見 …」,連到成立它的決議紀錄。

## 狀態標記

模組索引的狀態欄:

- ✅ —— 已蒸餾成完整段落,可以當規格讀。
- 🟡 —— skeleton,只有骨架,下次收斂時補完。

## 怎麼維護

見 [`../README.md`](../README.md) 的收斂迴路,與
[`.agents/skills/spec-workflow/SKILL.md`](../../.agents/skills/spec-workflow/SKILL.md)。
